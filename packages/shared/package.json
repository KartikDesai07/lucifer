{
  "name": "@pos/shared",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Shared spine (Zod schemas, constants, utils, API envelope, CRUD hook factory, the storage⇄presentation codec landed in F2c) reused by the cafe runtime and the owner Hub — single source of truth, no duplication (build-rule #31, CODE 3).",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/codec.test.ts src/ttl-guard.test.ts src/schemas/table.schema.test.ts src/product-import.test.ts"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0",
    "zod": "^3.24.2"
  },
  "peerDependencies": {
    "@tanstack/react-query": "^5.83.0",
    "next": ">=15",
    "react": ">=19",
    "sonner": "^2.0.7"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
