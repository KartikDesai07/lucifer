---
name: audit-findings
description: Results of the deep 117-agent code audit (2026-06-21) — grade, P0 fix list, and reframed Phase 8/0 plan
metadata:
  type: project
---

# Deep Code Audit — 2026-06-21

Ran a 117-agent read-only audit (10 subsystem maps + 8 web-research streams + 8
review dimensions + adversarial verify + synthesis). **86 findings confirmed, 4
refuted.** Visual report artifact: https://claude.ai/code/artifact/8b2db43e-87e7-40cc-abf0-80f94cb44919
Source HTML: scratchpad/audit-report.html (regenerable from this file's data).

## Verdict
**Grade B−.** Architecture + conventions are genuinely strong (uniform API layer,
correct global DB-connection cache, sound edge/Node auth split, IST date math,
shared Zod types). ~80% to the owner's vision. The weak 20% is concentrated on the
**money path** and **multi-instance serverless safety** — none of it needs
re-architecting; nearly every P0 is small + localized.

## ✅ PHASE 0 (all P0) IMPLEMENTED & build/lint/type-verified — 2026-06-22
All 8 P0 items below are DONE. Files touched:
- `lib/db.ts` — pool options (maxPoolSize 10 etc.) + `mongoose.set(sanitizeFilter/strictQuery)`.
- `next.config.ts` — security headers (no CSP yet — deliberate, CSP-with-nonce is a follow-up).
- `auth.config.ts` — `trustHost:true`. `scripts/seed-admin.ts` — requires strong SEED_ADMIN_PASSWORD, no default, no echo.
- `app/api/upload/route.ts` — DELETE scoped to CLOUDINARY_FOLDER.
- `models/Counter.ts` (NEW) — atomic per-day order seq (`nextOrderSequence`/`bumpOrderSequenceTo`).
- `lib/settings.ts` (NEW) — cached atomic singleton `getSettings`/`gstConfigOf`/`invalidateSettingsCache`; `app/api/settings/route.ts` refactored to use it (kills singleton race).
- `lib/receipt.ts` — `computeOrderTotals` (server source-of-truth bill math).
- `schemas/order.schema.ts` — base `orderObject` + `createOrderSchema` superRefine (Due/Credit need customer); `updateOrderSchema=orderObject.partial()`.
- `app/api/orders/route.ts` — POST fully rewritten: server recompute money, `derivePayment` (Split sum / paid-vs-total), atomic counter w/ reseed recovery, best-effort idempotent customer+table follow-ups (never 500 after commit), conditional table-grab, receiver from session.
- `app/api/orders/[id]/route.ts` — `clampLedger` ($max floor) on PUT/DELETE reconcile.
- `schemas/customer.schema.ts` — removed visits/totalSpend/totalDue (server-owned) + max lengths.
- `app/api/customers/[id]/reconcile/route.ts` (NEW, admin) — rebuild ledger from Orders.
- `app/api/customers/[id]/settle/route.ts` (NEW, admin) — clear dues (replaces PUT totalDue:0).
- `hooks/use-customers.ts` — `useSettleCustomerDue` + create success toast; `components/reports/CustomerDuesTable.tsx` rewired to settle endpoint; `CustomerFormSheet`/`CustomerSearch` drop financial fields.

NOT committed (working tree only). Known deferrals → P1: PUT money-recompute (no UI path sends item edits today), client idempotency key for true dedup, true stale-while-revalidate, CSP. NEXT = Phase 1 (P1).

## ✅ PHASE 1 (P1 — scoped subset) IMPLEMENTED & build/lint-verified — 2026-06-22
Done this session (the items named in the resume prompt). Files touched:
- `app/(dashboard)/error.tsx` (NEW) + `app/global-error.tsx` (NEW) + `app/(dashboard)/loading.tsx` (NEW) — first error/loading boundaries; prod hides raw `error.message` (dev-only detail + digest); global-error re-applies fonts via new `lib/fonts.ts`.
- `lib/fonts.ts` (NEW) — shared Geist instances; `app/layout.tsx` refactored to use `fontVariables`.
- `hooks/use-orders.ts` — optimistic create now writes to the `['orders','list',f]` keys views actually read (was a dead write to `['orders']`); **match-aware** insert (only lists the order's status/tableNo/payment/date match — no phantom rows); multi-list snapshot/rollback; shared `mutationKey` (ORDER_KEYS.mutation) on create/update/delete; `useIsMutating` guard pauses poll+focus refetch during writes; `refetchOnWindowFocus` ON.
- `hooks/use-tables.ts` — same isMutating guard (keyed on ORDER_KEYS.mutation) + `refetchOnWindowFocus` ON. (Introduces an intentional ESM-safe circular import with use-orders — keys used only in hook bodies.)
- `auth.config.ts` — `session.maxAge = SESSION_MAX_AGE_SECONDS` (8h); jwt stamps `lastValidated` on sign-in.
- `lib/auth.ts` — Node jwt callback re-validates role/isActive vs Mongo; returns `null` to clear cookie when deactivated/deleted (verified vs @auth/core); throttle backed by **node-cache marker** keyed by account id (cookie round-trip doesn't persist on plain `auth()` calls, so token.lastValidated alone can't throttle API traffic); transient DB error keeps existing token.
- `lib/constants.ts` — `SESSION_MAX_AGE_SECONDS` (8h), `SESSION_REVALIDATE_MS` (60s). `types/next-auth.d.ts` — JWT `lastValidated?`.
- `app/api/staff/[id]/route.ts` — `guardLastAdmin` blocks demote/deactivate of the last active admin on PUT+DELETE; PUT also blocks self-demote/deactivate (mirrors DELETE self-guard). TOCTOU count→write race documented as accepted at single-cafe scale.
- `app/api/staff/[id]/reset-password/route.ts` (NEW, admin-only) + `resetPasswordSchema` + `useResetStaffPassword` + `components/staff/ResetPasswordDialog.tsx` + staff page wiring (KeyRound action) — admin can reset a locked-out staff password.

Hardened via a 15-agent adversarial review (4 dims → verify): 6 findings confirmed, all fixed (auth throttle via node-cache; optimistic match-filter; PUT self-guard; TOCTOU note; global-error font; prod error-message hygiene). NOT committed (working tree only).

## ✅ PHASE 1 (P1 — remaining items) IMPLEMENTED & build/lint-verified — 2026-06-22
The four remaining P1 items are now done. Files touched:
- **GST snapshot on Order** — `models/Order.ts` + `types/index.ts` add `gstRate?`/`gstMode?` (snapshot of the GST config in effect at sale time). `app/api/orders/route.ts` POST persists `gstRate` (0 when GST off) + `gstMode` (server-set; NOT in the Zod schema, so the client can't spoof them). `lib/receipt.ts` `receiptGst` builds an "effective" config from the order's snapshot when present, falling back to live settings for legacy orders → receipts stay correct after the cafe later changes its rate/mode. `components/pos/OrderReceipt.tsx` GSTIN header now gated on the snapshot-aware `gst?.show` (review fix) so it can't drift from the tax body.
- **Bookings isError branches** — new `components/shared/ErrorState.tsx` (icon/title/desc + Retry). `reservations` + `events` pages add an `isError` branch (loading→error→empty→data) wired to `refetch()`, so a failed fetch no longer shows the misleading "No X" empty state.
- **Report date-range validation** — new `schemas/report.schema.ts` `reportRangeSchema` (YYYY-MM-DD format + real-date round-trip [rejects 2026-02-30 etc., review fix], start≤end, span ≤366d via `MAX_REPORT_RANGE_DAYS`). `app/api/reports/route.ts` validates the resolved range (defaults applied first, so omitting params never 400s) → 400 on a bad/reversed/over-wide range. Barrel-exported in `schemas/index.ts`.
- **Bundle trims** — `next.config.ts` `experimental.optimizePackageImports` (recharts, date-fns, lucide-react). `components/providers.tsx` devtools now `process.env.NODE_ENV==='production' ? null : dynamic(...)` so the package is DCE'd from the prod client bundle. `app/(dashboard)/pos/page.tsx` PaymentModal + ModifierModal via `next/dynamic({ssr:false})` (PaymentResult kept as `import type`).

Hardened via an 8-agent adversarial review (4 dims → per-finding skeptic verify): 4 raised, 2 confirmed + fixed (GSTIN header drift; calendar-overflow date acceptance), 2 correctly rejected (dynamic-modal loading fallback = accepted single-cafe tradeoff; redundant-but-harmless lucide-react entry). NOT committed (working tree only).

**P1 COMPLETE.** Remaining deferrals (→ P2/later): PUT `/api/orders` money-recompute (no UI sends item edits today; snapshot fields stay consistent because `updateOrderSchema` excludes them); client idempotency key for true dedup; cross-isolate stale-while-revalidate; CSP-with-nonce. NEXT = Phase 2 (P2 code health): Reservations≈Events dedup, CRUD route/hook factories, shared FormField/FormSheet, STALE_TIMES single source, 3 GET routes connectDB-before-cache, 3 files >300 lines.

## ✅ PHASE 2 (P2 — code health) IMPLEMENTED & build/lint/type-verified — 2026-06-22
All P2 items done + adversarial-review-hardened. Files:
- **STALE_TIMES single source** — new `lib/query.ts` (`STALE_TIMES`, `GC_TIMES`, `REFETCH_INTERVALS`). All 10 query hooks (products/categories/staff/customers/reservations/events/orders/tables/settings/reports) source staleTime/gcTime/refetchInterval from it — no more scattered magic numbers.
- **CRUD hook factory** — new `hooks/create-crud-hooks.ts` `createCrudHooks<T,Create,Update,F>` → `{useList,useCreate,useUpdate,useRemove}` (standard toast text + onSettled invalidation; optional `listKey`/`buildListQuery` for filtered lists; `extraInvalidate` per-op). Adopted by use-products/categories/staff/reservations/events (full) + use-customers (mutations only; search/orders/settle stay bespoke). Public hook names + `*_KEYS` unchanged. NOTE: customer CREATE invalidation moved onSuccess→onSettled (consistency; safe — only an extra refetch on a failed create).
- **CRUD route factory** — new `lib/crud-route.ts` `createCollectionRoute`/`createItemRoute`/`bookingListFilter`. Reservations≈Events now share one filter helper + factory. Adopted by products(+[id]), categories(GET/POST only), reservations(+[id]), events(+[id]). Still bespoke (correctly): categories/[id] (rename→products propagation + reassign-on-delete), staff/customers/orders/tables/settings (select/bcrypt/search/optimistic/singleton). `[id]` routes destructure only the verbs they expose (reservations/events = `{PUT,DELETE}`; products = `{GET,PUT,DELETE}`).
- **connectDB-before-cache fixed in all 3 GET routes** — reservations + events via the factory (cache.get before connectDB; cached only when unfiltered); customers fixed inline (search → connectDB; non-search → cache.get first, connectDB only on miss).
- **Shared FormField + FormSheet** — new `components/shared/FormField.tsx` (label+control+inline error, optional htmlFor) + `FormSheet.tsx` (Sheet shell + form + footer submit/spinner). 5 sheets refactored: Product/Customer/Reservation/Event/Staff. `overflow-y-auto` preserved on product/reservation/event; absent on customer/staff.
- **3 files >300 lines split** — orders/page→`components/orders/OrderTable.tsx` (page now 206); pos/page→`hooks/use-pos-totals.ts` + `components/pos/MobileCartBar.tsx` (page now 275; `Cart.tsx` now exports `CartProps`); ProductFormSheet→`components/products/ModifierInput.tsx` (now 214). No non-ui source file exceeds 300 lines.

Hardened via a 9-agent adversarial review (5 dims [hooks/routes/forms/splits/dedup] → per-finding skeptic verify): 2 confirmed + fixed — (a) EventFormSheet "Advance" field now surfaces its validation error + `aria-invalid` (parity with "Payable"); (b) removed dead `onDuplicate`/`extraCacheDel` from `createItemRoute` config (no item route used them; categories/[id] stays bespoke). NOT committed (working tree only).

**P2 COMPLETE.** Remaining deferrals (→ P3/later): PUT `/api/orders` money-recompute; client idempotency key; cross-isolate stale-while-revalidate; CSP-with-nonce. NEXT = Phase 3 (P3 growth): bulk CSV import wizard, live floor/open-tables dashboard panel + richer KPIs, held/open-table orders + fire-KOT-before-payment, stock decision, product archive/restore, order search by phone, printable end-of-day summary, README.

## ✅ PHASE 3 (P3 growth) — Step 1: Bulk CSV product import — IMPLEMENTED & build/lint/type-verified — 2026-06-22
First P3 growth feature done + adversarial-review-hardened. Files:
- **lib/product-import.ts (NEW)** — pure/isomorphic (no Node/DOM/Mongoose; imported by both the browser dialog and the server schema): `IMPORT_COLUMNS`, `MODIFIER_SEPARATOR="|"`, `MAX_IMPORT_ROWS=1000`, alias-aware `normalizeHeader`/`normalizeRow` ("Price (₹)"/"discount %"/"is_active" all collapse to canonical keys; unknown headers ignored), `coerceProductRow` (CSV strings→types: ""→undefined so Zod default/required fires; unparseable number→raw string so Zod reports a type error; pipe-split modifiers; true/yes/1/active ↔ false/no/0/inactive booleans), `buildTemplateCsv`.
- **schemas/product.schema.ts** — `importProductRowSchema = z.preprocess(coerceProductRow, createProductSchema)` (REUSES createProductSchema — there is no second notion of "valid product") + `importProductsSchema {dryRun, rows: z.record(string,unknown)[].min(1).max(1000)}`. Barrel + types exported.
- **app/api/products/import/route.ts (NEW)** — POST `requireAuth` (matches the staff-accessible products POST/PUT/DELETE). Per-row safeParse → dedupe-by-name (last wins; earlier same-name rows flagged `duplicate`, keeps the upsert collision-free) → classify create vs update vs existing names → `dryRun:true` returns a per-row preview + summary{valid/invalid/toCreate/toUpdate/duplicates} + newCategories WITHOUT writing; commit upserts via `Product.bulkWrite(updateOne filter{name} $set upsert, ordered:false)` (created=upsertedCount, updated=matchedCount) and auto-creates missing categories (`Category.bulkWrite $setOnInsert order=maxOrder+i`). cache.del('products'/'categories'). Mirrors the seed-menu.ts upsert-by-name pattern (name in filter only; Mongoose 8 applies timestamps to bulkWrite).
- **hooks/use-product-import.ts (NEW)** — `useImportPreview` (dry-run, no cache effects) + `useImportProducts` (commit; onSettled invalidates PRODUCT_KEYS + CATEGORY_KEYS).
- **components/products/ImportProductsDialog.tsx + ImportPreviewTable.tsx (NEW)** — upload→preview→result wizard. PapaParse (client) `header:true, skipEmptyLines:greedy`; template download; per-row verdict table (create/update/duplicate/error badges + messages); summary badges + new-categories note; Import disabled when valid===0. Dialog `next/dynamic({ssr:false})`-imported on the products page so PapaParse (~45kB) is code-split out of /products First Load.
- **lib/export.ts** +`downloadCsvString` (raw-string CSV download); **types/index.ts** +`ImportRowResult`/`ImportPreview`/`ImportResult`/`ImportResponse`; **app/(dashboard)/products/page.tsx** + Import button + dynamic dialog.
- DEPS added: `papaparse` + `@types/papaparse`. **xlsx deliberately NOT installed (unpatched CVEs).**
- **SEMANTICS (intentional, documented in upload-step UI copy):** upsert-by-name uses a full `$set` → a row whose name matches an existing product OVERWRITES every column (omitted optional columns reset to schema defaults; "CSV is source of truth"). Missing categories are auto-created (products store a denormalized category name).
Hardened via a 13-agent adversarial review (5 dims [correctness/validation/security/UX/conventions] → per-finding skeptic verify): **8 raised, 3 confirmed + fixed** — (a) [med] reset dialog state on OPEN not close (the dialog stays mounted; a late-resolving request's per-call onSuccess could leave a stale preview/result on reopen); (b) [low] result screen now reports `duplicates` so it reconciles with the preview (added to ImportResult); (c) [low] surface PapaParse structural `res.errors` (column-shift) as a non-blocking `toast.warning`. **5 correctly rejected** (blank-name "Required" vs custom message = cosmetic; pre-cap body-size DoS = project-wide validateBody convention, not this route's bug; dead `ImportResponse` export kept as the response-contract type; `RawRow` 1-token alias dup; duplicate-accounting = same as fix b). Checks: `tsc --noEmit` ✓ · `lint` ✓ · `build` ✓ (only the pre-existing harmless jose Edge `CompressionStream` warning; `/products` First Load 210kB — PapaParse code-split). **NOT committed (working tree only).** NEXT P3: live floor/open-tables dashboard panel + richer KPIs (Collected vs In-progress, Open Tables N/8, real Outstanding Dues, hourly); held/open-table orders + fire-KOT-before-payment; stock decision; product archive/restore; order search by phone; printable end-of-day summary; README.

## ✅ PHASE 3 (P3 growth) — Step 2: Live floor panel + richer KPIs — IMPLEMENTED & build/lint/type-verified — 2026-06-23
Second P3 feature done + adversarial-review-hardened. Decided WITH the user (AskUserQuestion): **6 KPI cards** + **view-only floor panel with click-to-open-order**. Files:
- **app/api/orders/summary/route.ts (REWRITTEN)** — now fetches ALL of today's orders (was `status:Completed` only) + a parallel `Customer.aggregate({totalDue:{$gt:0}})` (sum+count) in one `Promise.all`. `completed` = in-memory filter. Completed-only: `totalSales`/`totalOrders`/`paymentBreakdown`(byMode)/`topProducts`/**`collected`** (sum paidAmount). `inProgress` = today's Pending `{count, value=Σtotal}`. `outstandingDues` = the live ledger `{total, customers}` (ALL customers, date-independent). `hourly` = completed orders bucketed by `cafeHourOf` into a CONTIGUOUS min..max hour range (zero-filled gaps); `[]` when no completed orders. Dropped the old today-only `totalDue`. Cached 30s under `orderSummaryCacheKey()`.
- **lib/utils.ts** +`cafeHourOf(date)` (IST hour 0-23, UTC-host-safe like cafeDateString/dayRange). **types/index.ts** — reshaped `OrderSummary` (−totalDue; +collected, +inProgress{count,value}, +outstandingDues{total,customers}, +hourly) + new `HourlyStat{hour,sales,orders}`.
- **components/dashboard/SummaryCard.tsx** +`indigo`/`purple` variants (static `bg-{c}-50 text-{c}-600` strings — purge-safe). **HourlySalesChart.tsx (NEW)** — recharts BarChart, X=`formatHour` ("2 PM"), Y=sales(inr), EmptyState when empty; dynamic-imported (code-split). **LiveFloorPanel.tsx (NEW)** — compact 8-tile floor from `tables[]` + today's `orders[]`; header `occupied/total` + Manage→/tables; occupied tile whose `currentOrderId` resolves in `ordersById` → `<button>` opening the shared OrderDetailSheet, else non-clickable `<div>`; loading=8 skeletons, isError=destructive text, **empty=EmptyState** (review fix #5).
- **app/(dashboard)/page.tsx (REWIRED)** — dropped the separate `useOrders({status:'Pending'})` query (replaced by summary.inProgress); added `useTables()`. **6-KPI grid** (`grid-cols-2 sm:grid-cols-3 xl:grid-cols-6`): Today's Sales, Orders Served, Collected, In-progress, Open Tables (`occupied/total` from tableList, loading=tables.isLoading), Outstanding Dues. +`plural()` helper. Added `<LiveFloorPanel>` + a "Sales by hour" card with dynamic `<HourlySalesChart>`. KPIs read `s?.field ?? 0`. `/` First Load 166 kB (recharts code-split).
- **Cache consistency:** `customers/[id]/settle` + `reconcile` routes now `cache.del(orderSummaryCacheKey())` (they move `Customer.totalDue` → the Outstanding Dues KPI); orders `[id]` PUT+DELETE also del today's key when a ledger was touched (review fix #3 — the ledger-wide dues figure lives in TODAY's summary regardless of the order's own date).
Hardened via an 18-agent adversarial review (5 dims [summary-correctness/cache-ledger/react-data-layer/ui-conventions/perf-freetier] → per-finding skeptic verify): **5 confirmed + fixed, 8 rejected**. Fixes: (1+2) **`collected` made completed-only** to match the reports endpoint's "Collected" (was all-orders → same label, two numbers for the same day) + accurate comments; (3) order PUT/DELETE del today's summary key on ledger touch; (4) **`REFETCH_INTERVALS.SUMMARY` 2min→30s** (the live KPIs were lagging the 30s floor/recent widgets beside them; server already caches 30s) + fixed stale "caches 2min" comment in use-orders.ts; (5) LiveFloorPanel EmptyState on empty tables. Rejected (correctly): inProgress/collected "double-count" (no figure sums them; and #1's fix removes the overlap anyway), order-PUT prior-day key (subsumed by fix #3), nested optional-chaining crash (no client persistence + server never emits partial + type forbids it), magic-number 8 / 0-8-on-error (existing idiom, cosmetic transient), bare-id tooltip, no-`.select()` projection (cheap at scale; single-fetch-and-partition is deliberate), Customer.totalDue COLLSCAN (cheap + cached + pre-existing pattern), double-read of today's orders (non-redundant; proposed `.select()` fix would break the detail sheet). Checks: `tsc --noEmit` ✓ · `lint` ✓ · `build` ✓. **NOT committed (working tree only). In-browser verify pending.** NEXT P3: held/open-table orders + fire-KOT-before-payment; stock decision; product archive/restore; order search by phone; printable end-of-day summary; README.

## ✅ PHASE 3 (P3 growth) — Step 3: Open tabs / running orders + fire-KOT-before-payment — IMPLEMENTED & build/lint/type-verified — 2026-06-23
Third P3 feature. Decided WITH the user: research-grounded (108-agent deep-research on Toast/Square/Lightspeed/Posist/PetPooja → the accurate cross-product model is Open→Closed = our Pending→Completed, explicit fire action, multi-round KOT firing only new items, settle-frees-table, one open order per table; merge/transfer/by-seat-split/tip-state DEFERRED as enterprise bloat). Plan approved via ExitPlanMode. Files:
- **lib/order.ts (NEW)** — shared server money/ledger lib: `derivePayment` (moved from orders POST; +`Unpaid→{paidAmount:0}` branch), `ledgerContribution(order)` (Unpaid→{0,0,0}; else {visits:1,spend:total,due:max(0,total−paid)}), `reconcileLedger(old, updated|null)` (applies C(updated)−C(old) w/ same-customer & customer-change branches + `clampLedger`; updated=null for delete), `gstConfigFromOrder(order, liveFallback)` (rebuild GstConfig from the order's gstRate/gstMode snapshot), `validCustomer`.
- **app/api/orders/[id]/settle/route.ts (NEW)** — server-authoritative settle. derivePayment vs STORED old.total (no item recompute); Due/Credit require an EXISTING customer (resolves + `Customer.exists` check — review fix, else a deleted-customer attach loses the due); order's customer authoritative, body only ATTACHES to a customerless tab (name snapshotted server-side); conditional `findOneAndUpdate({_id,status:"Pending",total:old.total})` → double-settle applies the ledger delta once AND a raced-in add can't cause a stale underpayment (loser 409); `reconcileLedger`; frees table (currentOrderId===orderId); dels summary(today + tab-day)+customers+tables.
- **app/api/orders/[id]/items/route.ts (NEW)** — fire a KOT round: stamp new items kotRound=kotRounds+1, recompute FULL item set via `computeOrderTotals([...old.items,...new], discount, gstConfigFromOrder(old,live))` (tab's GST snapshot, not live), keep paidAmount0/Pending/Unpaid; optional discount rides along (re-clamped). Conditional `findOneAndUpdate({_id,status:Pending,payment:Unpaid,kotRounds:old.kotRounds})` → a concurrent add or a settle-that-landed-first 409s (no silent lost round, no append to a settled bill). dels today's summary (inProgress value grew).
- **Refactors:** orders POST imports derivePayment + uses ledgerContribution (held tab w/ customer ⇒ no ledger touch) + stamps round 1; [id] PUT/DELETE use reconcileLedger; `updateOrderSchema` hardened to `.pick({customerName,customerId,tableNo,notes}).partial().strict()`; new `settleOrderSchema`/`addItemsSchema` (.strict(), reuse orderItemSchema, kotRound server-stamped). summary route paymentBreakdown unchanged (already the 5 modes) but `OrderSummary.paymentBreakdown` re-typed `Record<SettlementPayMode,PaymentStat>`; dashboard `paymentSlices` maps `SETTLEMENT_PAY_MODES` (else `s.paymentBreakdown["Unpaid"]` crash). Model: Order.kotRounds + OrderItem.kotRound.
- **POS:** `hooks/use-pos-tab.ts (NEW)` owns the order/tab orchestration (create/add-round/settle, resume hydration, print signals) → page stays <300 lines. `useCart` +`kotRound` on CartItem, `hydrate()`, merge-guard (adds only fold into unfired lines), `cartItemFromOrderItem`/`cartItemToInput`. Cart: "Send to Kitchen"(hold) + "Pay Now"(immediate) for new orders; on a resumed tab "Send round N" + "Settle & Pay" (Settle **disabled while unsent items exist** — review fix, else they'd be dropped from bill+kitchen); fired lines locked under "Sent", new under "New". `OpenTabsButton` (NEW, `useOrders({payment:"Unpaid"})`) resumes a tab (confirm if a fresh cart is in progress — review fix). KOT prints only the fired round (`KOTReceipt` +roundItems/roundLabel). `optimisticOrder` honors input.status. `useAddOrderItems`/`useSettleOrder` (invalidate orders+tables+customers+summary). TableSelector locked during resume.
- **Settle from lists:** OrderDetailSheet shows "Settle & Pay" for Pending (owns a PaymentModal + useSettleOrder; `onSettled(updated)` refreshes the parent snapshot so the sheet shows the closed order + Print works — no stale/unmount race; server frees the table, no client updateTable). OrderTable Pending rows → Settle action (opens sheet); Due column suppressed for non-Completed (review fix — an open tab isn't a due). Dashboard In-progress KPI links to `/orders?payment=Unpaid` (orders page seeds the filter from the URL on mount). "Mark complete" removed everywhere (completion only via /settle).
- DEPS: none. PaymentModal +`modes?`(default SETTLEMENT_PAY_MODES)/`confirmLabel?`. SummaryCard +`href?`.
Hardened via a multi-agent adversarial review (5 dims → skeptic verify); **3 confirmed + fixed** (settle-drops-unfired-items P1; settle-attach-deleted-customer-loses-due P2; resume-discards-new-cart P3) + 1 self-caught (OrderTable due-for-open-tab) + 2 races proactively guarded (settle-stale-total, items-lost-round). The concurrency-cache review dimension + several verifiers were cut off by a session limit; self-verified the settle/items routes end-to-end after fixes (double-fire blocked via isBusy; cache dels complete). Checks: `tsc --noEmit` ✓ · `lint` ✓ · `build` ✓. **NOT committed (working tree only). In-browser verify pending.**
Accepted single-cafe tradeoffs (documented in code): discount on a resumed tab applies on the next fire (settle modal shows the authoritative stored total); Due/Credit settle of a *walk-in* tab from the orders sheet is blocked (no CustomerSearch there — attach the customer from the POS). NEXT P3: stock decision; product archive/restore; order search by phone; printable end-of-day summary (surface abandoned open tabs); README.

## ✅ PHASE 3 (P3 growth) — Step 4: Stock model + archive/restore + phone search + EOD summary + README — IMPLEMENTED & build/lint/type-verified — 2026-06-23
Closes out P3 growth. Decided WITH the user (AskUserQuestion): stock model = **"86" availability toggle** (no quantity, no auto-decrement, no low-stock alerts); EOD summary lives on the **dashboard, admin-gated**. Files:
- **stock → available** (atomic): `Product.stock:number` → `available:boolean` (model + schema + types). "Out" read as `available === false` everywhere (legacy docs without the field read as available → NO data migration). POS `ProductGrid` disables the tile when `available===false`. `ProductFormSheet`: Stock field + "Active" switch removed; new "Available" switch; the form no longer manages `isActive` at all (edit payload omits it so editing can't un-archive — review fix). **`isActive` now means ARCHIVED only.**
- **archive/restore**: DELETE was already soft (isActive:false). Added `?archived=true` listFilter on products GET (flips baseFilter→{isActive:false}, bypasses cache); `use-products` `ProductFilters{archived?}` + listKey nested under `["products"]` + new `useArchiveProduct`/`useRestoreProduct`/`useSetProductAvailability`. Products page: Active|Archived `Select`, inline availability `Switch` (per-row disabled while its PUT is in flight — review fix), Archive/Restore actions. Table extracted to `components/products/ProductsTable.tsx` (page back under 300 lines).
- **CSV import**: `available` is deliberately **NOT** an import column (live "86" state, not a menu attribute — a re-import must not reset out-of-stock; review fix removed it from IMPORT_COLUMNS/aliases/coerce/template/$set). `stock` aliases dropped. **`LowStockAlert.tsx` + `LOW_STOCK_THRESHOLD` deleted** (no alerts in the 86 model); dashboard's dead `useProducts()` removed.
- **phone search**: `escapeRegex` lifted to `lib/utils.ts` (customers route shares it); orders GET `?phone=` → Customer.mobile reverse-lookup → `customerId $in` (explicit customerId wins; no-match→`[]`). `OrderFilters` +phone +limit; `orderMatchesFilters` returns false when phone set (can't verify optimistically). Orders page: debounced phone search Input.
- **EOD summary**: `RECEIPT_PAGE_STYLE` lifted to `lib/print.ts` (POS + OrderDetailSheet dedup). New `EndOfDaySummary` (80mm thermal printable: sales / payments-by-mode / dues / **open-tabs-unsettled list**) + `EndOfDayButton` (admin-gated via `useAuth` — NOT `AdminGuard`, which redirects; self-fetches summary+openTabs+settings; print gated on `summary && settings && openTabs.isSuccess` so it never prints a false "all settled" — review fix; openTabs `limit:200` so it can't under-count — review fix). Mounted in the dashboard header.
- **README** rewritten (overview / features / stack / env / scripts / structure / deploy; points to CLAUDE.md as internal SoT).
Hardened via a 12-agent adversarial review (5 dims → per-finding skeptic verify): **7 findings, 6 confirmed + fixed** — import-un-86 (P2); EOD-readiness-false-"all-settled" (P2); edit-un-archives (P3); availability-Switch-double-fire (P3); open-tabs-50-cap (P3); form-Switch-a11y (P3). 1 rejected (shared `restorePending` disable = acceptable Pattern A). Checks: `tsc --noEmit` ✓ · `lint` ✓ · `build` ✓. **NOT committed (working tree only). In-browser verify pending.** **P3 GROWTH COMPLETE.** Deferred (enterprise/later): client idempotency key, cross-isolate stale-while-revalidate, CSP-with-nonce, merge/transfer/by-seat-split-bill.

## P0 — fix before any feature (Phase 0 of the reframed plan)
1. **Recompute order money server-side** from items + saved prices + Settings GST (reuse `lib/receipt` computeExclusiveGst); reject/overwrite client total beyond ₹1. Add Zod superRefine for Split/Due/Credit/Cash. (`app/api/orders/route.ts:96`, `schemas/order.schema.ts`)
2. **Atomic per-day order-number counter** (new `Counter` collection, `findOneAndUpdate $inc upsert`) — replaces the racy find-max+3-retry loop that can 500 mid-rush. (`app/api/orders/route.ts:85-105`)
3. **Idempotent order follow-ups + ledger floor**: customer $inc + table occupy become best-effort (never 500 after the order row commits); `$max`/floor so counters can't go negative; add a reconcile endpoint. (`app/api/orders/route.ts:78-129`)
4. **DB pool options** in `lib/db.ts:31` — `maxPoolSize:10,minPoolSize:0,maxIdleTimeMS:270000,serverSelectionTimeoutMS:5000` (M0 500-conn cap). One line.
5. **Lock down schemas** — strip server-owned fields (customer visits/totalSpend/totalDue, staff role/isActive) from create/update, make `.strict()`, add audited admin-only "settle dues" endpoint. (`schemas/customer.schema.ts`, `app/api/customers/[id]/route.ts`)
6. **Drop node-cache TTLs sharply** + stale-while-revalidate (menu 15-30s, settings 30-60s, tables 5-10s) — per-isolate `cache.del()` does NOT invalidate peers, so staff see stale prices/86'd items. (`lib/cache.ts`)
7. **Conditional table-grab** — `findOneAndUpdate({tableNo,status:'Available'})`; surface "Table just taken" instead of clobbering currentOrderId. (`app/api/orders/route.ts:117-123`)
8. **Cheap hardening** — `mongoose.set('sanitizeFilter'/'strictQuery')`; scope Cloudinary DELETE to the cafe folder; require AUTH_SECRET + trustHost; require SEED_ADMIN_PASSWORD (no default `Admin@123`). + security response headers in next.config (Step 8.2).

## P1 (smoothness/never-break/UX) — Phase 1
error.tsx + global-error.tsx + loading.tsx (zero error/loading boundaries today —
a render throw white-screens the whole panel); fix the **dead optimistic update**
in `hooks/use-orders.ts` (targets `['orders']`, lists read `['orders','list',f]`)
+ isMutating guard + refetchOnWindowFocus on orders/tables; session maxAge 8-12h +
re-validate isActive/role + last-admin guard; **admin password reset** (staff
forget passwords, currently fully locked out); GST snapshot on Order; bookings
isError branches; report date validation; bundle trims (optimizePackageImports,
devtools out of prod, dynamic POS modals).

## P2 (code health) — Phase 2
Reservations≈Events are near-duplicate stacks; 11 CRUD hooks repeat one pattern
(useCreateCustomer already drifted — no success toast); extract CRUD route factory
+ hook factory + shared `<FormField>/<FormSheet>`; fix 3 GET routes that connectDB
before cache.get; STALE_TIMES single source; 3 files >300 lines.

## P3 (growth) — Phase 3
**Bulk CSV import wizard** ✅ DONE (Step 1, 2026-06-22 — see the PHASE 3 section above)
(PapaParse client, server re-validate per row, bulkWrite
upsert-by-name, dry-run preview, template — do NOT npm-install xlsx, unpatched CVEs);
**live floor/open-tables dashboard panel** + richer KPIs (Collected vs In-progress,
Open Tables N/8, real Outstanding Dues, hourly); held/open-table orders +
fire-KOT-before-payment; stock decision; product archive/restore; order search by
phone; printable end-of-day summary; README.

**How to apply:** This reframes Phase 8 — security hardening is now folded into a
broader Phase 0→3 plan. Do P0 first, fully, before features. Login rate-limiting
stays SKIPPED (see [[feedback-preferences]]). Roadmap: P0 alone lifts grade B−→B+.
